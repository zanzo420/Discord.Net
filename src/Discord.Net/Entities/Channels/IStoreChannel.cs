// todo: docs
namespace Discord
{
    // review: do we even want to support this? does it add any value?
    public interface IStoreChannel : INestedChannel
    {
        // do store channels contain any extra data? I think it's just the application_id, if anything
    }
}
